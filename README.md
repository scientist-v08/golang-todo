To run a go program:- CompileDaemon -command="./go-crud" // Here ./go-crud is the project we want to run.
Alternatively, we can also use go run main.go
To create a table:- go run migrate/migrate.go. Within the file migrate.go input the given table that you want to create.
